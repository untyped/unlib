#lang setup/infotab

(define name "unlib")

(define blurb 
  '("A set of utilities developed by Untyped."))

(define release-notes
  '("Added Scribble documentation and updated some identifiers to make them more consistent with PLT 4."))

(define url "http://svn.untyped.com/unlib/")

(define doc.txt "doc.txt")

(define scribblings '(("doc/unlib.scrbl" (multi-page))))

(define categories '(devtools))

(define required-core-version "3.99")
